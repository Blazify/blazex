class Main {
    @ property
    var a = 0;

    @ constructor
    fun() => { 
        a = a + 1;
        a_edit(5);
    }

    @ method
    fun a_edit(new_a) => {
    	a = new_a;
    }
}

var main_ins = new Main();
println(main_ins);

