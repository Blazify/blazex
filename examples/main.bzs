var a = 9;
fun sumToA(int) => {
    a = a + int;
    return a;
}


@ this will return false as it sets a to the nearest symbol_table and returns that,
@ whereas we are using the ctx in the parent symbol_table
println(sumToA(a));
println(a);