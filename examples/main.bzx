class A {
    var x = 0

    fun() {
        soul.x = 2
        return soul
    }

    fun y(r) {
        return soul.x + r
    }

    static var z = 2
}

@ keep all the code in this function for now until extern functions are a thing
fun MAIN() {
    var a = new A()
    var b = new A()

    b.x = 3
    return (fun() { return b.y(A.z + a.x)})()
}
