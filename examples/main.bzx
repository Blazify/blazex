@@
    Program that prints "Hello World!" 100 times
@@

@ declare a external function "puts"
extern fun puts(string): int;

@@
    you don't need this anonymous function wrapping the while loop, just
    was testing them.
@@
(fun(): fun(): int {
    fun(): int {
        var a = 0;
        while a < 100 {
            puts("Hello World!");
            a += 1;
        }
        printf("Times: %i\n", a);
        0
    }
})()()