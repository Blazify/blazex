extern variadic fun printf(string): int;

fun fib(n: int): int {
    if n <= 1 { 
        return n
    }

    return fib(n - 1) + fib(n - 2)
}

var a = 0;


for i = 0 to 20 step 1 {
    a += fib(i)
}


printf("%i\n", a)
